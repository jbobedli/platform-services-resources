{{- range $clave, $valor := .Values.sa.pipelines.namespaces }}
kind: ServiceAccount
apiVersion: v1
metadata:
  name: pipeline
  namespace: {{ $valor }}
  annotations:
    argocd.argoproj.io/sync-wave: "1"
{{- end}}