kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: sonarqube-routes
  namespace: {{ .Values.namespace }}
rules:
  - verbs:
      - '*'
    apiGroups:
      - route.openshift.io
    resources:
      - '*'
